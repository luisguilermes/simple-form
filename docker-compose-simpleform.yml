version: "3"
services:

  proxy:
    build: ./proxy
    links:
      - simpleformapp
    ports:
      - "80:80"
    environment:
      - NGINX_HOST=app
      - NGINX_PROXY=http://simpleformapp:9090

  app:
    build: ./app
    environment:
      ENV: PROD
    volumes:
      - ./app/src:/app
